# Scripts

Utility scripts are grouped by purpose:

- `server/`: start or restart local, phone, and temporary online access.
- `diagnostics/`: smoke checks for API routes and provider configuration.

The root `.bat` files are small double-click wrappers for the main server scripts.
